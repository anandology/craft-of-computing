#! /bin/bash
#
# The first version on the new update system. It does nothing, so the move
# from the old tarball releases is tested on its own, before any version
# changes a student's machine.

echo "Switched to the new update system."
